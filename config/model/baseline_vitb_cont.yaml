# @package _group_
# Continuous (no hashing) counterpart of baseline_vitb_hash, for the ROADMAP arm.
name: RetrievalNet
freeze_batch_norm: False
freeze_pos_embedding: False
kwargs:
  backbone_name: dino
  dino_backbone: 'dinov2_vitb14'
  embed_dim: 768
  norm_features: False
  without_fc: True
  with_autocast: False
  pretrained: True
